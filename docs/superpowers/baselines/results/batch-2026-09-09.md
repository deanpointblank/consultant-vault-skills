# Seven-scenario batch — control run, 2026-09-09

Three reps per scenario, `general-purpose` agents, fresh context, existing plugin installed,
no new skills. Scored from the vault copies, not the agents' summaries.

## Scenarios where the control already passed

The vault holds exemplars for three genres, and every rep found and copied them:

| Scenario | Result | What carried it |
|---|---|---|
| handoff | 3/3 chain shape: `type: handoff`, where-everything-is table, rulings section, gated section, previous handoff set `superseded`, AC8 answer opened as a question owned by Rob | 16 existing handoff notes |
| time-logging | 3/3 exemplar sections, per-day table, assumptions flagged as provisional, no Harvest export stated, zero external writes (calendar, Jira search, Granola list all read-only) | two existing time-logging notes and their embedded rules |
| shareable export | 3/3 zero wikilinks, zero Stride names, `audience: external`, headings preserved | the existing Draft/Shareable pair |
| daybook routing | 3/3 `owns` on the person, `owners` on the repo, glossary note, `#blocker`, gotcha into the dossier | the daybook and people skills as they stand |

Per writing-skills, guidance for a failure the control does not exhibit is not authored. But
these three genres pass only because this vault has exemplars; a fresh engagement has none.
The honest baseline is a copy with the exemplars removed, run as a second control below.

Daybook's one miss in 3/3: the ticket key stayed prose. No `jira` property anywhere.

## Scenarios where the control failed

### sync-simulated (3 reps)

| Check | Result |
|---|---|
| S1 precedent applied (`working-session`) | 3/3, read from the Sync State prose |
| S2 mistranscriptions resolved in a callout | 3/3 |
| S3 alias stored on the person note | 0/3 |
| S4 inferred roster carried as a property | 0/3, callout only |
| S5 sync log one line, above the exclusions heading | 0/3: all three appended a paragraph **below** `## Excluded from sync`, reproducing the live vault's defect |
| S6 decision notes for the two rulings | 0/3 |
| S7 Ray's Friday absence to his person note | 0/3 |

Verbatim, identical in all three: **"a sync doesn't create those"** (decision notes), followed
by an offer to log them if asked. Two of three also bumped `granola_last_sync` without having
queried Granola; the third explained why it did not.

### gardener (3 reps)

| Check | Result |
|---|---|
| G2 glossary coverage of high-mention terms | 0/3 |
| G3 private body marker without `visibility` property | 0/3 |
| G4 `ticket`/`jira`, `artifact`/`artifacts` drift | 1/3 |
| G5 filename genre vs `type` drift | 0/3 (all listed the off-schema types; none noticed a Handoff typed `research`) |
| G6 sync log split across the exclusions heading | 0/3 |
| G1, G7, G8 (severity grouping, Fatima alias, no changes) | 3/3 |

The reports were long and careful. They checked exactly what the skill lists and nothing
else; every new check was absent in every rep.

### clipper-local (3 reps)

| Check | Result |
|---|---|
| C1 `Clippings/` with `type: clipping` | 3/3 |
| C2 provenance (`source_url` as `file://`, `author`, `source_date`) | 3/3 |
| C6 original copied into the vault | **0/3**: `source_url` points at the scratch path the file came from |

## Edits made against these failures

- granola-sync: `meeting_type_rules` in config with a write-back rule; aliases stored on
  person notes; `attendees_confirmed: false` for inferred rosters; `visibility: private`
  for 1:1s; decision notes created during the sync (step 8); personal facts to person
  notes; one-line run log under `## Runs` above the exclusions; `granola_last_sync`
  bumped only after a real query; `granola_excluded` documented.
- decisions: "From meetings" section stating promotion is part of the write-up.
- daybook: ticket keys go into the daily note's `jira` list.
- vault-gardener: six new checks (glossary coverage, private markers, property drift,
  type drift, sync log integrity).
- reference-clipper: local-files section; copy the original into attachments first.
- property-schema: `attendees_confirmed`; `type: ticket`.
- Config template: `meeting_type_rules`, `tickets` folder.
- people: `aliases` purpose.

## No-exemplar control (handoff, time-logging, shareable)

Copies with the exemplar notes deleted (all root `*Handoff*`, `Time Logging*`, `*Shareable*`).

**handoff, 3/3 in:** shape held up without exemplars (state table, rulings, open items,
numbered pick-up list, AC8 question owned by Rob, 09-04 question closed by Rob's rewrite),
but variance appeared where a template would pin it: one rep invented `decided_by` /
`ratified_by` properties; filenames differed; none set `supersedes`; none created a ticket
note. Skill written: `skills/handoff/` with Handoff and Ticket templates.

**shareable, 3/3 in:** redaction clean in all three (zero wikilinks, zero Stride names, scrub
grep reported). Three filenames, three property sets (`status: shareable`; `audience` +
`recipients` + `source_note`; `audience` + `source`), one back-link to the source, one daily
line. Skill written: `skills/shareable/`.

**time-logging, 3/3 in (Opus, after the Fable session limit):** read-only in all three
(calendar, Jira search, Granola list, git log; zero writes), honest about the unevidenced
Tuesday, but three different `type` values (`reference`, `proposal`, `time-log`), three
heading sets, per-day narrative instead of one allocation table, and no worklog-id column.
Skill written: `skills/time-logging/` with a template; schema gains `type: time-log` and
`type: handoff` rows.

## GREEN (sync, daybook, gardener, clipper)

**daybook, 3/3:** `jira: [PFD-66381]` stamped on the daily note; routing unchanged from
control. Two reps noted no skill creates ticket notes yet; the handoff skill now does.

**clipper, 3/3:** original copied into `Attachments/`, `source_url` pointing at the copy.

**sync, 3/3 (2 Fable, 1 Opus):** `attendees_confirmed: false` set; aliases written to the
person note; two decision notes (one Opus rep filed the reload-endpoint ruling as a conflict
instead, because the vault records no role model, and linked it from Notes); Ray's absence
to his person note; run line above the exclusions heading and the two stray live-vault lines
moved back; `granola_last_sync` left alone because Granola was not queried. Two of three
added `meeting_type_rules` to config; the third declined because the precedent already
existed, so the rule now says a precedent that lives anywhere but config gets moved there.

**gardener, 3/3 (Opus):** glossary coverage table with counts (V1, V2, Phenix, Vector,
EWM, UAA, DT as a bare alias miss), the private 1:1s without the property, `ticket`/`jira`
and `artifact`/`artifacts` drift with majority named, Handoff-typed-`research` drift, the
sync-log split and the six stale flags. One rep also found a plaintext password in
`PFD Environments - Test Login.md` with no visibility flag.

## GREEN (handoff, shareable, time-logging; Opus)

| Scenario | Checks | Result |
|---|---|---|
| handoff | identical seven sections, `status: current`, `supersedes` set, ticket note created, AC8 question owned by Rob, no invented decision keys, daily line | 3/3 |
| shareable | skill filename, `audience` + `source` + `recipients`, zero body wikilinks, zero Stride, source back-link, daily line, grep reported | 3/3 |
| time-logging | `type: time-log` with period bounds, six template sections, attribution line, worklog-id column, per-day subtotals, unattributed rows instead of padding, "assumed" stated, filed in Tickets, zero external writes | 3/3 |

**Model note.** Controls ran on Fable 5.1. After the session limit, the time-logging
controls and every GREEN rep for handoff, shareable, time-logging, gardener, and one sync
rep ran on Opus. The failures being tested are shape and omission failures that the Fable
controls exhibited and the Fable-run GREEN reps (daybook, clipper, two sync) also cleared,
so the model switch is a confound on magnitude, not direction. A same-model rerun of one
scenario would settle it.

## Status 2026-09-09, end of batch

All ten items have a skill or edit and a passing GREEN run. Still untested: description-based
triggering for every new skill (all GREEN reps were pointed at the working copy). Nothing is
committed.

**Live-vault follow-ups the runs exposed** (user's call, not applied):
- `visibility: private` on the Dean/Chris 1:1 notes and on `PFD Environments - Test Login.md`,
  which holds a plaintext password.
- Move the two stray run lines in `Meta/Sync State.md` above the exclusions heading.
- Add `conflicts`, `tickets` folders and `meeting_type_rules` to `Meta/Config.md`; copy the
  Conflict, Handoff, Ticket, and Time Logging templates.
- Retype the four Handoff notes carrying `research`/`reference` and the three Time Logging
  notes carrying `reference`; rename `ticket:` to `jira:` on the one holdout.

## GREEN (sync, daybook, gardener, clipper)

(filled after the run)

## Trigger test 2026-09-11 (handoff, shareable, time-logging, reconcile)

Description-based triggering, the item left open above. `claude -p --model sonnet`, one fresh session per prompt, plugin `58a7e6b` installed from GitHub, cwd `uscold-map`, vault copy under the session scratchpad with `OBSIDIAN_VAULT` set and one prompt line saying the copy is deliberate. No skill named in any prompt. Pass = the skill is the first tool call.

| Skill | Typed | First tool call | First line of the reply | Vault writes (copy) |
|---|---|---|---|---|
| handoff | `write the handoff for PFD-65947 so I can pick it up cold tomorrow` | `handoff` | "Handoff written: `Tickets/PFD-65947 Handoff - PRs Awaiting Review 2026-09-11.md`, superseding yesterday's …" | new handoff, ticket note and prior handoff updated, daily line |
| shareable | `make a version of "Appointments Reference Data - Feeder Options 2026-09-02" I can send to Heiko and Reto` | `shareable` | "Done. The shareable copy is at: `Appointments Reference Data - Feeder Options - Shareable 2026-09-02.md`" | the export, source note linked, daily line |
| time-logging | `do my time logging for this week so far, draft only, I'll post the worklogs myself` | `time-logging` | "Draft written to `Time Logging - Week of 2026-09-07.md`, logged in today's daily note. 15.83 hours so far …" | the draft note, daily line; nothing posted |
| reconcile | `I have 30 minutes with Ray before he sends ADR-001 to Chris for sign-off. What do I need to raise with him? Keep it to what matters.` | `reconcile` | "Filed two new findings in the vault before answering — the Conflicts folder didn't exist yet, so I created it …" | `Conflicts/` with two notes |

Four of four fired. The sync, daybook, gardener, and clipper edits were not trigger-tested: they changed existing skills' bodies, not their descriptions.
